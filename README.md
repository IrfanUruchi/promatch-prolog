# promatch-prolog
ProMatch is an SWI-Prolog web app for B2B event matchmaking. It manages events, participants, roles, timeslots and availability blocks, then generates conflict-free schedules (Greedy, CLP(FD) Optimal, Stable pairing). Includes CSV export and optional Cerebras AI intro generator.
